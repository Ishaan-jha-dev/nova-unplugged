// Root page.tsx — redirect to (public)/page.tsx which handles "/"
// The App Router will resolve "/" to (public)/page.tsx automatically.
// This file intentionally left empty to avoid route conflict.
export { default } from './(public)/page'
